# What is this

This repo documents attempts to invent a computational method of finding repeated sequences in genomes using self-to-self blast and graph-based clustering. The method is not yet finalized and is work in progress. The main idea is to use blast to find all pairwise matches of repeats in a genome and then use graph-based clustering to group them into "families" and find therefore number of such families and their sizes (number of members).
